# Stop pnpm 11 from running `pnpm install` before every script.
verify-deps-before-run=false

# Skip the post-install build-script approval prompt for known-safe packages
# (sharp, esbuild, protobufjs, etc.). Their build scripts are mainstream.
dangerously-allow-all-builds=true

auto-install-peers=true
